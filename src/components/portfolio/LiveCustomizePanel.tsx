/**
 * LiveCustomizePanel.tsx
 *
 * NOTE: The standalone trigger button has been merged into EditModeToolbar
 * (Appearance tab). This file is kept for backward compatibility but renders
 * nothing — the functionality now lives inside EditModeToolbar.tsx.
 */

const LiveCustomizePanel = (_props: { profileId: string }) => null;
export default LiveCustomizePanel;
